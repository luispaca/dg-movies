<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= movie.title %></title>
    <link rel='stylesheet' href='/css/style.css' />
    <link rel="icon" type="image/webp" href="img/dh.webp" sizes="16x16"/>
</head>
<body>
    <figure>
        <img src="/img/logo-DH.png" alt="Logo Digital House">
    </figure>
    <h1><%= movie.title %></h1>
    <p>RATING : <%= movie.rating %></p>
    <p>AWARDS : <%= movie.awards %></p>
    <p>LENGTH : <%= movie.length + ' min '%></p>
    <p>RELEASE DATE : <%= movie.release_date %></p>
    <p>GENRE: <%= movie.genreName %></p>
    <p>ACTORS: <%= 
        movie.actors.map((actor) => {
        return actor.firstName + ' ' + actor.lastName
    }).join(', ') %></p>
    <br>
    <button><a href="/movies">Volver</a></button>
    <button><a href="/movies/edit/<%= movie.id %>">Editar</a></button>
    <button><a href="/movies/delete/<%= movie.id %>">Borrar</a></button>
</body>
</html>